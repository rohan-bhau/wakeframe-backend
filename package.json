{
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore \"mocha --require tsx/cjs tests/firestore.rules.test.ts\""
  },
  "dependencies": {
    "firebase": "^10.14.1"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "@types/mocha": "^10.0.0",
    "@types/node": "^22.0.0",
    "mocha": "^11.0.0",
    "tsx": "^4.19.0"
  }
}
